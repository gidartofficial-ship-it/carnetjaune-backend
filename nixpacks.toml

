[phases.setup]
nixPkgs = ['nodejs_20', 'openssl_3']

[phases.build]
cmds = [
  'npm install --production',
  'npx prisma generate',
  'npx prisma db push --accept-data-loss'
]

[start]
cmd = 'node server.js'